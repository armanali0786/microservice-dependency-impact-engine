# microservice-dependency-impact-engine
A developer platform that builds a live dependency graph of distributed applications and predicts the blast radius of code, API, database and infrastructure changes, while correlating runtime failures back to affected dependencies.
